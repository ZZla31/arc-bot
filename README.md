# arc-bot （转漆华老师）


pip install -r requirements.txt
playwright install chromium
python arc_daily.py

# 安装依赖
pip install -r requirements.txt
playwright install chromium
# 虚拟环境 
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
playwright install chromium
python arc_daily.py

# 编辑文本
nano accounts.txt （账号邮箱）
nano gmail_passes.txt （谷歌邮箱专用密码）
nano proxies.txt （代理，本地的话填none）

# 注意
谷歌邮箱IMAP设置为停用自动清除
